
import 'package:flutter/material.dart';

class ListaRecomendacaoUserPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Scaffold(
      appBar: AppBar(
        title: Text('Recomendações sobre Usuario'),
      ),
      body: Center(
        child: Text('Tela de lista de recomendações sobre o usuario'),
      ),
    );
  }
}

class UserResumeModel {

  String userName;
  String userImageUrl;
  String userLocation;
  String userId;
  String userProfile;

  UserResumeModel(this.userName, this.userImageUrl, this.userProfile, this.userId, this.userLocation);

}
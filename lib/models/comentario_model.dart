

class Comentario{

  String userName;
  String userId;
  String userImageUrl;
  String descricaoComentario;
  String dataComentario;

  Comentario( this.userName, this.userId, this.userImageUrl, this.descricaoComentario, this.dataComentario);
}